{
  "schema_version": 1,
  "metrics": {
    "production_loc": 2390,
    "production_files": 26,
    "production_packages": 8,
    "test_files": 9,
    "production_functions": 95,
    "decision_points": 122
  },
  "persistence": {
    "module": "go.etcd.io/bbolt",
    "entities": ["AccessCard", "Video", "EmployeeSession", "AuditRecord"],
    "reopen_test": "TestPersistenceSurvivesReopen"
  },
  "workflows": [
    {"steps": ["Employee submits normalized card code", "Gateway checks card state", "Gateway persists usage", "Gateway creates session"], "test": "TestWorkflowOne"},
    {"steps": ["Employee presents session", "Gateway checks freshness", "Gateway filters published catalog", "Gateway authorizes direct video"], "test": "TestWorkflowThree"},
    {"steps": ["Employee starts session", "Employee cancels", "Workflow checks cancellation", "Gateway refuses final commit"], "test": "TestBusinessChain23"},
    {"steps": ["Administrator chooses prefix", "Gateway generates cards", "Administrator requests report", "Gateway returns status totals"], "test": "TestReport"}
  ]
}
